info:
  name: dweet
  type: http
  seq: 2

http:
  method: GET
  url: https://dweetr.io/dweet/for/ing301?params=message=Lecture still ongoing
  params:
    - name: params
      value: message=Lecture still ongoing
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
