
# Set this to a suitable value for your tasks

def square(x: int) -> int:
    """
    Return x^2 for a number x.
    """
    raise NotImplementedError

def max2(a: int, b: int) -> int:
    """
    Return the maximum of two numbers.
    """
    raise NotImplementedError

def max3(a: int, b: int, c: int) -> int:
    """
    Return the maximum of three numbers.
    """
    raise NotImplementedError

def average(a: int, b: int) -> float:
    """
    Return the arithmetic mean of two numbers.
    """
    raise NotImplementedError

def average3(a: int, b: int, c: int) -> int:
    """
    Return the arithmetic mean of three numbers.
    """
    raise NotImplementedError


def factorial(n: int) -> int:
    """
    Return n! for natural number n (0! = 1).
    <https://en.wikipedia.org/wiki/Factorial>
    Make some assumption about what should happen when the input is negative.
    Try to implement this function both in a recursive and then in an iterative manner.
    """
    raise NotImplementedError

def product(lower: int, upper: int) -> int:
    """
    Return the product of all integers from lower to upper (inclusive).
    """
    raise NotImplementedError


def sum(lower: int, upper: int) -> int:
    """
    Return the sum of all integers from lower to upper (inclusive).
    """
    raise NotImplementedError

def fib(n: int) -> int:
    """
    Return Fibonacci(n) with fib(0)=fib(1)=1; for n>1, fib(n)=fib(n-1)+fib(n-2).

    """
    raise NotImplementedError

def is_prime(n: int) -> bool:
    """
    Returns bool if the given input is a prime number, it is divisible by exactly two numbers (1 and itself).
    """
    raise NotImplementedError

def reverse(n: int) -> int:
    """
    Reverses the given input number, e.g. reverse(1234) == 4321.
    For educational purposes: Please do not "cheat" by turning the number into a string, reversing the string, and turning back to integer.
    If you are stuck: please revisit the module and whole division operators '%' and '//', and think about the meaning of
    our sitional number system, i.e. "1234" actually means "1 * 10 ^ 3 + 2 * 10 ^ 2 + 3 * 10 ^ 1 + 4 * 10 ^ 0".
    """
    raise NotImplementedError
